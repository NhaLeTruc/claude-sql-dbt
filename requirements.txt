# dbt dependencies with version pinning for reproducibility
# Using minor version ranges to allow patch updates while preventing breaking changes
dbt-core>=1.10.13,<1.11.0
dbt-postgres>=1.9.1,<1.10.0
